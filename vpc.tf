# VPC configuration is managed using existing VPC
# VPC data source defined in data.tf
# Variables defined in variable.tf
# To specify VPC and subnet IDs, use terraform.tfvars or pass via CLI:
# terraform apply -var="vpc_id=vpc-xxxxx" -var='subnet_ids=["subnet-xxxxx","subnet-yyyyy"]'
